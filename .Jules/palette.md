## 2026-06-18 - Missing aria-labels on icon-only buttons
**Learning:** The application's custom UI patterns frequently use icon-only buttons (e.g., leveraging the `.btn-icon` class) that natively lack accessibility context, making them unusable for screen reader users.
**Action:** Regularly scan for and add `aria-label` attributes to these elements when implementing UX improvements.
