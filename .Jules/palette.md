## 2024-07-19 - Missing ARIA Labels on Icon Buttons
**Learning:** The `btn-icon` class is frequently used for icon-only buttons across HRMS components without `aria-label` attributes, making them inaccessible to screen readers.
**Action:** Always verify that elements with the `btn-icon` class include descriptive `aria-label` attributes for screen reader accessibility.
