## 2024-07-21 - [Added ARIA Labels]
**Learning:** Found an accessibility issue pattern specific to this app's components, where icon-only buttons using the `.btn-icon` class consistently lack required `aria-label` attributes.
**Action:** Always verify elements with the `.btn-icon` class have appropriate `aria-label` attributes when working on this app's components.
