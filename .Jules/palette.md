## 2024-09-21 - Missing ARIA Labels on Icon-Only Search Buttons
**Learning:** Icon-only buttons used for search submissions are missing `aria-label` attributes, making them inaccessible to screen readers.
**Action:** Always verify that buttons containing only an icon (`btn-icon`) have descriptive `aria-label` attributes, especially for common actions like search.
