## 2026-07-13 - Missing aria-labels on icon buttons
**Learning:** The codebase, specifically within `src/components/hrms/` uses the `.btn-icon` class for icon-only buttons that lack required `aria-label` attributes.
**Action:** Add `aria-label` attributes to icon-only buttons to improve screen reader accessibility.
