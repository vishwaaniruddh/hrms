## 2024-06-10 - ARIA Labels for Icon Buttons
**Learning:** The application's custom UI patterns frequently use icon-only buttons natively lacking accessibility context.
**Action:** Regularly scan for and add `aria-label` attributes to elements with classes like `.btn-icon`, `.nk-quick-nav-icon`, `.nk-nav-toggle`, and `.dropdown-toggle` when implementing UX improvements.
