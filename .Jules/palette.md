## 2024-05-24 - Missing ARIA labels on icon-only actions
**Learning:** Icon-only action buttons (like toggles and dropdowns) in this app's profile components frequently lack `aria-label`s, causing screen readers to read them as blank or unhelpful icons.
**Action:** Always verify icon-only buttons have descriptive `aria-label`s for screen reader support.
