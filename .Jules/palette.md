## 2026-06-24 - Added ARIA labels to icon-only buttons
**Learning:** The application's custom UI patterns frequently use icon-only buttons (e.g., leveraging the `.btn-icon` and `.nk-quick-nav-icon` classes) that natively lack accessibility context.
**Action:** Add `aria-label` attributes to these elements when implementing UX improvements to ensure keyboard and screen reader accessibility.
