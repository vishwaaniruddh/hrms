## 2026-06-26 - Add ARIA Labels to Header Navigation Icons
**Learning:** The application's custom UI patterns frequently use icon-only buttons (e.g., leveraging the `.nk-quick-nav-icon` class) that natively lack accessibility context.
**Action:** Regularly scan for and add `aria-label` attributes to these elements when implementing UX improvements.
