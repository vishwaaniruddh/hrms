## 2024-05-24 - React Code Splitting Pattern
**Learning:** Route components in `src/App.jsx` are dynamically imported using `React.lazy()` and `<Suspense>` to reduce initial bundle sizes.
**Action:** Always use this pattern for new route components added to `src/App.jsx`.
